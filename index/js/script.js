document.addEventListener('DOMContentLoaded',()=>{const menuToggle=document.querySelector('.menu-toggle'),navLinks=document.querySelector('.nav-links'),navButtons=document.querySelector('.nav-buttons');menuToggle.addEventListener('click',()=>{navLinks.classList.toggle('active');navButtons.classList.toggle('active');});const slides=document.querySelectorAll('.slide'),prevButton=document.querySelector('.slider-nav.prev'),nextButton=document.querySelector('.slider-nav.next');let currentSlide=0;function showSlide(n){slides[currentSlide].classList.remove('active');currentSlide=(n+slides.length)%slides.length;slides[currentSlide].classList.add('active');}prevButton.addEventListener('click',()=>showSlide(currentSlide-1));nextButton.addEventListener('click',()=>showSlide(currentSlide+1));setInterval(()=>showSlide(currentSlide+1),5000);slides.forEach((slide,index)=>{const catalogButton=slide.querySelector('.catalog-button'),productButton=slide.querySelector('.product-button');if(catalogButton){catalogButton.addEventListener('click',()=>{window.location.href='catalog_basses.html';});}if(productButton){productButton.addEventListener('click',()=>{window.location.href='product-details.html?id=2';});}});
});
